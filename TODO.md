# TODO

- [x] Update slideshow-popup.js: change click popup from single-image view to scrollable photo gallery (with images list/grid, selected image, scroll to current index).
- [x] Update style.css: add/adjust styles for scrollable gallery inside popup (max-height, overflow, grid/list layout, selected state).
- [x] Validate in browser: click slideshow image opens scrollable gallery; scroll works; close works; keyboard navigation works (Escape + arrows).



